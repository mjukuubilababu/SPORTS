# Unified Repository Architecture v0.1

Canonical flow:

Provider evidence -> Gate1 truth/provenance -> Gate2 feature/model backfill -> Gate3 statistical validation -> Gate4 robustness/champion-challenger -> Gate5 forward/paper execution -> Gate6 capital/risk.

The verified JavaScript reference E2E slice is retained as an independent reference implementation of the end-to-end stateful workflow, including signal freeze, security checks, idempotency, settlement, evaluation, audit and assurance.

This repository intentionally does not synthesize the five infrastructure packages that were built in another conversation branch. Their original artifacts must be imported before full-platform assurance can be PROMOTE.
